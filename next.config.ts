import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: {
  //   ppr: 'incremental'
  // } this for partial-prerendering. Is not released yet. pnpm install next@canary
};

export default nextConfig;
